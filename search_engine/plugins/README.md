# Search plugins

`official/` stores snapshots from the qBittorrent organization search-plugin
repository. `community/` is reserved for plugins selected from the community
catalog after explicit review.

Downloaded plugins are never imported automatically by Live Torrent Client.
Before enabling one, verify its source, license, network behavior, file access,
subprocess usage, and credential handling.
